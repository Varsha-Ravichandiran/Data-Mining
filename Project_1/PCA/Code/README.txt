1) Place the datasets in a folder
2) Specify the path of that folder in the code while reading the
datasets
3) Run the code in cmd using the command "python code.py"
4) Each plot will be generated one by one